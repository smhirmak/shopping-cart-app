export interface IProduct {
  id: number;
  title: string;
  description: string;
  images: string[];
  price: number;
  category: string;
  rating: number;
  thumbnail: string;
  stock: number;
  quantity: number;
  brand: string;
}
